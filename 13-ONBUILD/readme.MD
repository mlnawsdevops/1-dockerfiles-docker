#### ONBUILD instruction image

0. someone created image
1. first we ask build image id (onbuild:v1) 
2. we use build image id (onbuild:v1) as FROM os in user Dockerfile
3. next, we must pass the ONBUILD instruction file like index.html 
4. then, build the image 